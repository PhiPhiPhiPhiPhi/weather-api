import {Head, Html, Main, NextScript} from 'next/document';

/**
 * Load the page.
 *
 * @returns {any} The page.
 */
const Document = () => (
    <Html lang="en">
        <Head />
        <body>
            <Main />
            <NextScript />
        </body>
    </Html>
);

export default Document;